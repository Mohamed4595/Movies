package com.moviesList.presentation

import androidx.compose.material.Text
import androidx.compose.runtime.Composable
import coil.ImageLoader

@Composable
fun MoviesList(imageLoader: ImageLoader,
) {
    Text(text = "test")
}